export const products = (state) => state.products.data;
export const singleProduct = (state) => state.products.product.data;
export const productsLoading = (state) => state.products.inProgress;
export const productLoading = (state) => state.products.product.inProgress;
